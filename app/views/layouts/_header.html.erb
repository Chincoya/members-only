

<nav id='header'>
  <h1 class="d-inline"><%= link_to  'The Gossip House', root_url %></h1>
  <div class="header-links">
    <%= link_to 'Posts', index_path, method: :get, class: 'btn btn-danger' %>
    <% if logged_in? %>
      <%= link_to 'Log out', logout_path, method: :delete, class: 'btn btn-danger' %>
    <% else %>
      <%= link_to 'Log in', login_path, method: :get, class: 'btn btn-danger' %>
    <% end %>
  </div>
</nav>